spring:
  application:
    name: config-service
  cloud:
    config.server.git:
      uri: https://github.com/Harish-Palaspagar/lovable-config-server
      username: Harish-Palaspagar
      password: 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
      default-label: main
server.port: 8888
management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false

---

spring:
  config:
    activate:
      on-profile: k8s
  cloud:
    discovery:
      enabled: false
    config.server.git:
      uri: https://github.com/Harish-Palaspagar/lovable-config-server
      username: ${GIT_USERNAME}
      password: ${GIT_PASSWORD}
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false